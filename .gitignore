# Vim
*~
*.bak

#Goland
.idea/

# macOS
.DS_Store

# Vscode
.vscode

# Terraform
.terraform
*.tfstate
*.tfstate.backup
*.tfvars
*.tfstate*
resolved_models.json
# ...but commit the non-secret config tfvars so CI has it (secrets stay in env vars)
!deploy/terraform.tfvars

# Secrets sourced as env vars (keep env.sh.example, ignore the real one)
env.sh
.env
scratch.txt
